# AUDIT_REPORT — Кофейня «Уголёк», MVP формы обратной связи

Дата: 16.09.2026
Объект аудита: `D:\123vbcode_rf\Ch1` (server.js, public/index.html, reviews.json, ТЗ T3_MVP.md, документы 01/02)
Правило: ничего не подтверждается без наблюдаемого доказательства.

## Итоговое решение

**НЕ утверждается «готов к публикации MVP».**

Пункты 1, 3, 4, 6 подтверждены с доказательствами. Пункты 2 и 5 подтвердить нельзя:
- п. 2 «доступ к данным ограничен» — не выполняется на текущей конфигурации;
- п. 5 «сценарии из ТЗ работают» — серверный контур подтверждён, но браузерные сценарии (два браузера, 360 px, Chrome/Edge/Safari) в этой сессии не наблюдались.

Необходимые условия для утверждения: закрыть доступ по сети (см. п. 2) и провести живое браузерное демо.

---

## Пункт 1. В клиентском коде и URL нет секрета — ПОДТВЕРЖДЕНО

Доказательства:
- Поиск по `server.js` и `public/index.html` паттернов `password|passwd|secret|token|api[-_]?key|bearer|sk-|private[-_]?key` — 0 совпадений.
- В `public/index.html` нет ни одного `https?://`, `src=` или `href=` (grep по `(https?://|src=|href=)` — 0 совпадений): CSS и JS инлайновые, единственные URL относительные (`/`, `/api/reviews`).
- `server.js` не содержит учётных данных; `reviews.json` исключён из репозитория (`.gitignore`).
- Наблюдение вне пункта: в `D:\123vbcode_rf\opencode.json` (вне репозитория Ch1, не клиентский код MVP) есть Bearer-токен MCP-сервера. Значение не воспроизводится здесь. При любой публикации папки `D:\123vbcode_rf` содержимое этого файла не должно попасть в открытый репозиторий.

## Пункт 2. Доступ к данным ограничен — НЕ ПОДТВЕРЖДЕНО

Что ограничено (наблюдаемо):
- Роутов всего 4: `GET /`, `GET /index.html`, `GET /api/reviews`, `POST /api/reviews`; всё остальное — `404`.
- статики наружу не отдаётся: `GET /reviews.json` → 404, `GET /server.js` → 404 (путь к `public/index.html` зашит жёстко, каталоги не листаются).

Что НЕ ограничено (наблюдаемо):
- `server.listen(PORT, '0.0.0.0')` — сервер слушает все сетевые интерфейсы, доступен из локальной сети (риск И2 из аудита №1).
- `GET /api/reviews` отдаёт полный список отзывов, `POST /api/reviews` принимает отзывы без какой-либо аутентификации, авторизации и лимитов (риск И5: нет rate-limit). Отсутствие авторизации прямо зафиксировано в ограничениях проекта — но «доступ к данным ограничен» на этой конфигурации не выполняется.
- Вывод: до приведения доступа к ограниченному (bind на `127.0.0.1` либо закрытие порта 3000 для сети фаерволом) пункт остаётся неподтверждённым.

## Пункт 3. Права OpenCode минимальны — ПОДТВЕРЖДЕНО (на уровне конфигурации)

Доказательства:
- В `D:\123vbcode_rf\opencode.json`, `C:\Users\yab-1\.config\opencode\opencode.jsonc` и `C:\Users\yab-1\.config\opencode\Opencod.json` отсутствует секция `permission` (проверено по содержимому, в конфигах только `mcp`) → ни одного предзаданного `allow`; поведение — запрос разрешения на каждое действие (по умолчанию).
- В `D:\123vbcode_rf\Ch1` собственного конфига opencode (`opencode.json`/`.opencode/`/`AGENTS.md`) нет.
- Оговорка: в конфигах включены MCP-серверы (remote zerocoder, playwright, github с токеном через `{env:GITHUB_PERSONAL_ACCESS_TOKEN}`). Это расширенные возможности агента, но не автоматические права; их корректность не относится к клиентскому коду MVP.
- Не измерялось: фактическое поведение интерфейса подтверждения прав в живом клиенте.

## Пункт 4. Внешние зависимости проверены — ПОДТВЕРЖДЕНО

Доказательства:
- `server.js` (строки 1–3) использует только встроенные модули Node.js: `http`, `fs`, `path`.
- В репозитории нет `package.json` и `node_modules` (полный обход каталога — 7 файлов, сторонних компонентов нет).
- `public/index.html` не ссылается ни на какие внешние ресурсы (см. п. 1); сетевой трафик страницы — только относительный `/api/reviews`.
- Совместимо с критерием K10 (без внешних запросов/зависимостей) и K1 (работает локально без интернета).

## Пункт 5. Сценарии из ТЗ работают — НЕ ПОДТВЕРЖДЕНО ПОЛНОСТЬЮ

Проверено наблюдаемо (через HTTP, честный UTF-8, реальный рестарт):
- `GET /` → 200 (раздача страницы) — K1 (частично).
- `POST /api/reviews` валидный отзыв → 201, появляется в списке (Ф7, Ф8).
- Валидация: оценка 0 → 400; оценка 6 → 400; меню из 101 символа → 400; комментарий из 501 символа → 400; комментарий из повторяющихся символов → 400; меню-«тарабарщина» (латтница с нечитаемой раскладкой) → 400 (K3, новые правила защиты).
- Тело 1 МБ → 413 (защита И4).
- K6 (устойчивость к перезапуску): отзыв сохранился при полном перезапуске сервера — PID 16288 → 952, порт между запусками свободен, запись найдена по id → подтверждено.
- Неизвестный путь → 404.
- 360 px: в коде есть `<meta viewport>` и отзывчивые правила (нет фиксированных ширин, max-width 720px) — проверка только уровня кода, фактические рендер на 360 px не наблюдался.
- K5 (отзыв виден в двух браузерах), K1 «открывается в Chrome/Edge/Safari», hover/клик по звёздам — в этой сессии браузерное окружение недоступно, наблюдений нет.
- Вывод: серверный контур и хранилище подтверждены; браузерные сценарии требуют живого демо, поэтому пункт в целом не подтверждаю.

## Пункт 6. Ограничения проекта зафиксированы — ПОДТВЕРЖДЕНО

Доказательства (содержимое файлов):
- `01_Текущий процесс.md` — раздел «Уточнение (решение)»: централизованный сбор; ограничение «без серверной части» заменено на минимальный локальный Node.js-сервер (стандартная библиотека) с `reviews.json`.
- `02_Ограничения_и_материалы.md` — зафиксировано: localhost-демо, без платного хостинга, без npm/внешних пакетов/СУБД, без авторизации, писем, CRM, загрузки файлов, PDF, внешних AI API, только вымышленные данные.
- `T3_MVP.md` — критерии K1–K10 и функциональные требования Ф1–Ф13; отдельно зафиксированы решения по защите от мусорных текстов и лимитам полей.
- `.gitignore` — исключены `reviews.json`, `reviews.json.tmp`, `node_modules/`.

---

## Сводка

| № | Проверка | Вердикт |
|---|---|---|
| 1 | Секретов в клиентском коде и URL нет | Подтверждено |
| 2 | Доступ к данным ограничен | Не подтверждено |
| 3 | Права OpenCode минимальны | Подтверждено |
| 4 | Внешние зависимости проверены | Подтверждено |
| 5 | Сценарии из ТЗ работают | Не подтверждено полностью (сервер — да, браузер — нет) |
| 6 | Ограничения проекта зафиксированы | Подтверждено |

## Что нужно, чтобы утвердить «готов к публикации MVP»
1. Применить риск И2: bind на `127.0.0.1` (по умолчанию) или закрыть порт 3000 для локальной сети.
2. (Рекомендуется) риск И5: простой ограничитель частоты POST (например, не более ~10 запросов в минуту).
3. Живое демо: два браузера на одной машине (K5), Chrome/Edge/Safari (K1), вьюпорт 360 px (K8), выбор оценки звёздами.